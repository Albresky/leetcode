# [hard] #0022 合并 K 个升序链表

## 思路

合并 K 个链表，有两种思路：1）两两合并（法一、法二）；2）同时合并（法三）。

**法一：线性合并**

先构造个空链表，然后将 K 个链表依次两两合并到这个空链表上。

**法二：分治合并**

将 K 个链表 按 $\frac{K}{2}$ 左右两边合并，然后对于每半边，递归合并其内部的左半边和右半边，直到每个链表只剩一个节点为止。

**法三：优先队列合并**

将 K 个链表的头节点存入优先队列（小顶），每次队头合并进结果链表，然后将该节点的下一个节点入队，直到队列为空为止。


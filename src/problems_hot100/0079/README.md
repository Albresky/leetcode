# [mid] #0079 单词搜索

## 思路

**DFS + 回溯**

 - 先遍历出起始节点，再从起始节点出发，DFS 搜索
 - 需要一个 `visited` 数组，标记已经访问过的节点，防止重复访问
 - 每次访问一个节点后，标记为已访问，然后尝试四个方向
   - 如果找到，返回 true
   - 如果没找到，回溯，标记为未访问，继续尝试
   - 如果四个方向都没找到，返回 false
   - 如果 idx == word.length() - 1，说明已经找到所有字符，返回 true
 - 如果所有起始节点都没找到，返回 false